This repository is built around nix's `flake.nix`
and the following commands:

- `nix develop`
- `nix run . -- server/ target/`
- `nix build`


But there is no need to use nix, it works fine manually too.
